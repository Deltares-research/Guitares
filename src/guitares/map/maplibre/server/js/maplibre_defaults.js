/**
 * maplibre_defaults.js - Default map settings for the MapLibre backend.
 *
 * Identical structure to defaults.js; used when the map engine is
 * explicitly MapLibre (as opposed to Mapbox). Overwritten at runtime.
 */
default_style = 'mapbox://styles/mapbox/streets-v11';
default_center = [0.0, 0.0];
default_zoom = 2;
default_projection = 'mercator';
