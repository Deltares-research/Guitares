/**
 * defaults.js - Default map settings.
 *
 * These globals are overwritten at runtime by the Python-side configuration
 * before the map is created. They serve as fallback values only.
 */
default_style = 'mapbox://styles/mapbox/streets-v11';
default_center = [0.0, 0.0];
default_zoom = 2;
default_projection = 'mercator';
